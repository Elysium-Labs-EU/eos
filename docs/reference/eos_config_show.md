## eos config show

Print the effective config.yaml

### Synopsis

Print the effective daemon configuration: ~/.eos/config.yaml values merged over eos's built-in defaults.

```
eos config show [flags]
```

### Examples

```
  eos config show
```

### Options

```
  -h, --help   help for show
```

### Options inherited from parent commands

```
      --no-daemon   run in local mode without daemon
      --verbose     enable verbose debug logging
```

### SEE ALSO

* [eos config](eos_config.md)	 - Inspect and scaffold the eos daemon configuration

