# gitleaks purpose-built ignore mechanism (gitleaks detect -i/--gitleaks-ignore-path,
# default '.', auto-discovered at repo root): fingerprints below are commit-hash-keyed
# false positives from gitleaks' structural/entropy-based "private-key" rule, not
# something a regex allowlist (.gitleaks.toml) can express well.
#
# Both fingerprints are the deliberately-fake multi-line PEM key fixture used by
# cmd/diagnose_test.go's TestDiagnoseScrubLines_MultiLinePrivateKey and
# TestDiagnoseCmd_MultiLinePrivateKeyRedactedInRealServiceLog, which prove eos
# diagnose's log redactor actually scrubs a real PEM key logged one line at a time
# (the multi-line regression the previous review round flagged) — not a real key.
560d2af4ae7b0b34aa63aeab397b1d602f4d21c2:cmd/diagnose_test.go:private-key:567
560d2af4ae7b0b34aa63aeab397b1d602f4d21c2:cmd/diagnose_test.go:private-key:601
