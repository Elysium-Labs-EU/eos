# Config for https://github.com/crate-ci/typos
# Run locally: typos .   (install: cargo install typos-cli, or brew install typos-cli)

[files]
extend-exclude = [
  "CHANGELOG.md",  # generated by git-cliff from commit history; fix typos at the source commit instead
  "go.sum",
  "*.gif",
]
